namespace Spillgebees.Blazor.Map;

/// <summary>
/// Cascaded marker telling button components they render inside a
/// <see cref="ButtonGroupMapControl"/>, so they skip their own control shell.
/// </summary>
internal sealed class MapButtonGroupContext;
