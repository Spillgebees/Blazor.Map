@namespace Spillgebees.Blazor.Map

<div @ref="_placeholderReference" data-sgb-control-placeholder="@Id" class="sgb-map-custom-control-placeholder" hidden>
    <div id="@_contentId" @ref="_contentReference" class="sgb-map-custom-control-content" hidden>
        <div class="@_groupClass">
            <button type="button"
                    class="@_buttonClass"
                    aria-label="@Label"
                    title="@_effectiveTitle"
                    aria-pressed="@_ariaIsOn"
                    disabled="@Disabled"
                    @onclick="ToggleAsync">
                @if (_currentIcon is not null)
                {
                    <span class="sgb-map-control-icon" aria-hidden="true">
                        @_currentIcon
                    </span>
                }
                @if (!string.IsNullOrWhiteSpace(_displayText))
                {
                    <span class="sgb-map-control-text">@_displayText</span>
                }
            </button>
        </div>
    </div>
</div>
